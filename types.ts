import type { StackScreenProps } from '@react-navigation/stack';

type RootStackParamList = {
    Authorization: undefined
    Google: undefined
  };

  type Props = StackScreenProps<RootStackParamList, 'Authorization'>;

